# ucsc-places-converter
Converts a JSON feed containing UCSC places to XML

Translates the coordinates of any places that don't have an address, into addresses using the google maps reverse geocoding API
